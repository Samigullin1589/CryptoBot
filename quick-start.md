# 🚀 Быстрый старт: Деплой на Render за 5 минут

## ⚡ Шаг 1: Подготовка (2 минуты)

```bash
# 1. Скопируйте файлы в ваш проект
cp render.yaml runtime.txt ./

# 2. Проверьте готовность
python check_deploy_ready.py

# 3. Закоммитьте изменения
git add render.yaml runtime.txt
git commit -m "Add Render deployment config"
git push origin main
```

## ⚡ Шаг 2: Создание на Render (2 минуты)

1. **Зайдите на** → [dashboard.render.com](https://dashboard.render.com)
2. **Нажмите** → `New +` → `Blueprint`
3. **Выберите** → Ваш GitHub репозиторий `CryptoBot`
4. **Нажмите** → `Apply`

Render автоматически создаст:
- ✅ Web Service (ваш бот)
- ✅ Redis (кеш и состояния)

## ⚡ Шаг 3: Переменные окружения (1 минута)

Перейдите в **Web Service** → **Environment** и добавьте:

| Переменная | Где взять | Пример |
|-----------|-----------|--------|
| `BOT_TOKEN` | Telegram @BotFather | `1234567890:ABC...` |
| `ADMIN_IDS` | Ваш Telegram ID (@userinfobot) | `123456789,987654321` |

**REDIS_URL** - настраивается автоматически! ✓

## ✅ Готово!

Подождите 3-5 минут, пока Render развернет бота.

Проверьте в Telegram: `/start`

---

## 🐛 Если что-то не работает

**Бот не отвечает?**
```bash
# Проверьте логи в Render Dashboard → Logs
# Убедитесь, что BOT_TOKEN правильный
# Убедитесь, что бот не запущен локально
```

**Health check failing?**
```bash
# В bot/health_check_server.py должен быть endpoint /health
# Проверьте, что сервер запускается в main.py
```

**Redis error?**
```bash
# Убедитесь, что Redis создан в Blueprint
# Переменная REDIS_URL должна быть автоматической
```

---

## 📊 Что дальше?

- 📖 Полная инструкция: `DEPLOYMENT_GUIDE.md`
- 🔒 Безопасность: Не коммитьте `.env` файлы
- 💰 Бесплатный план: 750 часов/месяц
- 🔄 Автодеплой: При каждом push в GitHub

---

## 🆘 Нужна помощь?

- 📚 Документация Render: https://render.com/docs
- 💬 Community: https://community.render.com
- 📧 Support: support@render.com

**Готово! Ваш бот в облаке! 🎉**