# 📦 Пакет для развертывания CryptoBot на Render

Этот пакет содержит все необходимые файлы для развертывания вашего Telegram бота на платформе Render.

---

## 📁 Содержимое пакета

### 🔧 Конфигурационные файлы (скопируйте в корень проекта):

| Файл | Описание | Куда копировать |
|------|----------|-----------------|
| **render.yaml** | Конфигурация сервисов Render (бот + Redis) | `./render.yaml` |
| **runtime.txt** | Версия Python для Render | `./runtime.txt` |

### 📚 Документация:

| Файл | Описание |
|------|----------|
| **QUICK_START.md** | Быстрый старт - деплой за 5 минут ⚡ |
| **DEPLOYMENT_GUIDE.md** | Полная подробная инструкция 📖 |
| **TROUBLESHOOTING.md** | Решение проблем и частые ошибки 🔧 |
| **README.md** | Этот файл 📄 |

### 🛠️ Вспомогательные файлы:

| Файл | Описание |
|------|----------|
| **check_deploy_ready.py** | Скрипт проверки готовности к деплою |
| **production_settings_example.py** | Пример настроек для production |

---

## 🚀 С чего начать?

### Вариант 1: Быстрый старт (5 минут)
1. Прочитайте **QUICK_START.md**
2. Следуйте инструкциям

### Вариант 2: Полная инструкция (15 минут)
1. Прочитайте **DEPLOYMENT_GUIDE.md**
2. Следуйте всем шагам подробно

### Вариант 3: Просто делай
```bash
# 1. Скопируйте файлы
cp render.yaml runtime.txt ~/ВАШ_ПРОЕКТ/

# 2. Проверьте готовность
python check_deploy_ready.py

# 3. Закоммитьте
git add render.yaml runtime.txt
git commit -m "Add Render config"
git push

# 4. Создайте Blueprint на Render.com
# 5. Добавьте BOT_TOKEN и ADMIN_IDS
# 6. Profit! 🎉
```

---

## 📋 Пошаговый план действий

### Шаг 1: Подготовка проекта (5 минут)

```bash
# 1. Перейдите в папку вашего проекта
cd ~/path/to/CryptoBot

# 2. Скопируйте конфигурационные файлы
cp /path/to/downloaded/render.yaml .
cp /path/to/downloaded/runtime.txt .

# 3. Проверьте готовность
python /path/to/downloaded/check_deploy_ready.py

# 4. Исправьте проблемы, если есть
```

### Шаг 2: Обновление кода (если нужно)

Если в вашем проекте нет health check сервера, добавьте:

```python
# bot/health_check_server.py
import os
from aiohttp import web

async def health(request):
    return web.Response(text='OK', status=200)

async def start_health_server():
    app = web.Application()
    app.router.add_get('/health', health)
    
    port = int(os.getenv('PORT', 10000))
    runner = web.AppRunner(app)
    await runner.setup()
    site = web.TCPSite(runner, '0.0.0.0', port)
    await site.start()
```

И в `bot/main.py` запустите его:

```python
import asyncio
from bot.health_check_server import start_health_server

async def main():
    asyncio.create_task(start_health_server())
    await dp.start_polling(bot)

if __name__ == '__main__':
    asyncio.run(main())
```

### Шаг 3: Git (2 минуты)

```bash
# Добавьте файлы
git add render.yaml runtime.txt

# Закоммитьте
git commit -m "Add Render deployment configuration"

# Запушьте
git push origin main
```

### Шаг 4: Render Dashboard (3 минуты)

1. Зайдите на https://dashboard.render.com
2. Нажмите **New +** → **Blueprint**
3. Выберите ваш репозиторий **CryptoBot**
4. Нажмите **Apply**

### Шаг 5: Переменные окружения (2 минуты)

В созданном **Web Service** → **Environment**:

```bash
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_IDS=ваш_telegram_id,другой_id
```

### Шаг 6: Ожидание (3-5 минут)

Render автоматически:
- ✅ Установит зависимости
- ✅ Запустит Redis
- ✅ Запустит бота
- ✅ Проверит health check

### Шаг 7: Проверка

В Telegram:
```
/start
```

Должен ответить ваш бот! 🎉

---

## 🔍 Проверка перед деплоем

Запустите скрипт проверки:

```bash
python check_deploy_ready.py
```

Он проверит:
- ✅ Наличие всех файлов
- ✅ Структуру проекта
- ✅ Конфигурацию Render
- ✅ Версию Python

---

## 🗂️ Структура файлов в вашем проекте после копирования

```
CryptoBot/
├── bot/
│   ├── config/
│   ├── handlers/
│   ├── services/
│   ├── main.py
│   └── health_check_server.py  ← Убедитесь, что есть
├── data/
├── tests/
├── render.yaml                  ← Новый файл
├── runtime.txt                  ← Новый файл
├── requirements.txt
├── .env.example
├── .gitignore
└── README.md
```

---

## 💡 Что делает каждый файл?

### render.yaml
Главный конфигурационный файл для Render. Определяет:
- **Web Service** (ваш бот)
  - Версия Python
  - Команды запуска
  - Переменные окружения
  - Health check endpoint
- **Redis Service** (кеш и состояния)
  - 25MB бесплатно
  - Автоматическое подключение

### runtime.txt
Указывает Render, какую версию Python использовать:
- `python-3.11.9` (рекомендуется)
- Можно изменить на 3.9 или 3.10

### check_deploy_ready.py
Скрипт для проверки:
- ✅ Наличие всех необходимых файлов
- ✅ Правильность конфигурации
- ✅ Структуры проекта
- ⚠️ Потенциальных проблем

### production_settings_example.py
Шаблон настроек для production:
- Переменные окружения
- Redis конфигурация
- Webhook настройки
- Rate limiting
- Безопасность

---

## 🔐 Обязательные переменные окружения

Эти переменные **ОБЯЗАТЕЛЬНО** нужно добавить в Render:

| Переменная | Где взять | Пример |
|-----------|-----------|--------|
| `BOT_TOKEN` | @BotFather в Telegram | `1234567890:ABC...` |
| `ADMIN_IDS` | @userinfobot в Telegram | `123456789,987654321` |

Эти переменные **ОПЦИОНАЛЬНЫ**:

| Переменная | Назначение | По умолчанию |
|-----------|------------|--------------|
| `ANTHROPIC_API_KEY` | Для AI функций | Не требуется |
| `DATABASE_URL` | PostgreSQL (если нужен) | Не требуется |
| `LOG_LEVEL` | Уровень логирования | `INFO` |

---

## 📊 Ограничения бесплатного плана

### Web Service (Starter)
- ✅ 750 часов/месяц бесплатно
- ✅ Автоматический спящий режим после 15 мин неактивности
- ✅ Пробуждение при первом запросе (~30 сек)
- ⚠️ 512MB RAM
- ⚠️ Shared CPU

### Redis (Starter)
- ✅ 25MB storage бесплатно
- ✅ Persistence включен
- ✅ Без ограничений по запросам

**Итого:** $0-7/месяц в зависимости от использования

---

## 🆘 Если что-то пошло не так

### 1. Бот не запускается
→ См. **TROUBLESHOOTING.md** → Раздел 1

### 2. Health check failing
→ См. **TROUBLESHOOTING.md** → Раздел 2

### 3. Redis не подключается
→ См. **TROUBLESHOOTING.md** → Раздел 3

### 4. Бот не отвечает
→ См. **TROUBLESHOOTING.md** → Раздел 4

### 5. Другие проблемы
→ Читайте **TROUBLESHOOTING.md** полностью

---

## 📞 Куда обращаться за помощью

1. **Render Documentation**
   - https://render.com/docs

2. **Render Community**
   - https://community.render.com

3. **Render Support**
   - support@render.com

4. **Telegram Bot API**
   - https://core.telegram.org/bots/api

---

## ✅ Финальный чеклист

Перед деплоем убедитесь:

- [ ] `render.yaml` скопирован в корень проекта
- [ ] `runtime.txt` скопирован в корень проекта
- [ ] `.gitignore` настроен (не коммитит .env)
- [ ] Health check endpoint существует (`/health`)
- [ ] `requirements.txt` актуален
- [ ] Код запушен в GitHub
- [ ] BOT_TOKEN получен от @BotFather
- [ ] ADMIN_IDS известны
- [ ] Прочитали QUICK_START.md или DEPLOYMENT_GUIDE.md

---

## 🎯 Что дальше после успешного деплоя?

### Рекомендации:

1. **Настройте мониторинг**
   - UptimeRobot для проверки доступности
   - Render Notifications для алертов

2. **Переключитесь на Webhook**
   - Более эффективно чем polling
   - См. production_settings_example.py

3. **Добавьте CI/CD**
   - GitHub Actions для автотестов
   - Автоматический деплой после прохождения тестов

4. **Оптимизируйте использование Redis**
   - Устанавливайте TTL для ключей
   - Используйте кеширование

5. **Мониторьте логи**
   - Регулярно проверяйте Render Logs
   - Настройте алерты на ошибки

---

## 💬 Примеры команд после деплоя

### Проверка работы бота:
```bash
# В Telegram:
/start
/help
/menu
```

### Проверка health endpoint:
```bash
curl https://ваше-приложение.onrender.com/health
# Должно вернуть: OK
```

### Просмотр логов:
```bash
# В Render Dashboard → Logs
# Или через CLI:
render logs -f your-service-name
```

---

## 🎉 Поздравляем!

Если вы дошли до этого места и ваш бот работает - вы молодец! 🚀

Ваш Telegram бот теперь:
- ✅ Работает 24/7 в облаке
- ✅ Автоматически обновляется при push в GitHub
- ✅ Использует Redis для состояний
- ✅ Имеет health check для стабильности
- ✅ Готов к production использованию

---

## 📚 Дополнительные ресурсы

- [Render Documentation](https://render.com/docs)
- [Aiogram Documentation](https://docs.aiogram.dev/)
- [Redis Documentation](https://redis.io/docs/)
- [Telegram Bot API](https://core.telegram.org/bots/api)

---

**Успешного деплоя! 🚀**

*Создано с ❤️ для простого развертывания Telegram ботов*