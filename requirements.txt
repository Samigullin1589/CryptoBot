# ===============================================================
# Файл: requirements.txt (ПРОДАКШН-ВЕРСИЯ, АКТУАЛЬНО НА АВГУСТ 2025)
# Описание: Зафиксированные версии для воспроизводимых сборок на Render.
# Runtime: Python 3.12.x
# ===============================================================

# --- Ядро и фреймворк Telegram ---
aiogram==3.21.0           # актуальная 3.x (05 июля 2025)
pydantic==2.11.1          # стабильная ветка v2 (28 марта 2025)
pydantic-settings==2.10.1 # (24 июня 2025)
python-dotenv==1.1.1      # (23 июня 2025)

# --- Сеть, HTTP-клиент и ретраи ---
aiohttp==3.12.15          # (конец июля 2025)
aiohttp-retry==2.9.1
backoff==2.2.1

# --- Планировщик задач ---
APScheduler==3.10.4

# --- Парсинг и текст ---
beautifulsoup4==4.13.4
lxml==6.0.0
bleach==6.2.0             # deprecated, но актуальная безопасная версия
rapidfuzz==3.11.0
feedparser==6.0.11

# --- Научные пакеты и графики ---
numpy==2.1.1
matplotlib==3.9.1

# --- Изображения / OCR (если используется) ---
Pillow==11.3.0
pytesseract==0.3.13

# --- ИИ SDK (если используется внутри сервисов) ---
openai==1.99.9
google-generativeai==0.8.5  # EOL с 31.08.2025 — см. ОШИБКА.txt
async-lru==2.0.5