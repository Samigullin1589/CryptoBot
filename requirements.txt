# Файл зависимостей. Версии обновлены до актуальных и стабильных.
# Знак ~= позволяет устанавливать будущие патч-релизы, сохраняя совместимость.

# --- Ядро и фреймворк ---
aiogram~=3.9.0
pydantic~=2.8.0
python-dotenv~=1.1.0
loguru~=0.8.0

# --- Сетевое взаимодействие ---
aiohttp~=3.9.5
# Опциональные, но рекомендуемые зависимости для производительности aiohttp
cchardet~=2.1.7
aiodns~=3.2.0
# Полезно иметь для синхронных/асинхронных запросов в других частях проекта
httpx~=0.27.0

# --- База данных и кэширование ---
redis~=5.1.0

# --- Планировщик задач ---
apscheduler~=4.0.0

# --- Парсинг и обработка данных ---
beautifulsoup4~=4.13.0
lxml~=5.2.0

# --- Научные вычисления и графики (если используются) ---
matplotlib~=3.9.0
numpy~=1.26.5
scipy~=1.13.0
